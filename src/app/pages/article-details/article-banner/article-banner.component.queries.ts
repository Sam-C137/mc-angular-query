import { createFavoriteArticleMutation } from "../../home/article-list/article-list.component.queries";
import { createProfileMutation } from "../../profile/profile-banner/profile-banner.component.queries";

export {
    createFavoriteArticleMutation,
    createProfileMutation,
};
